package com.example.weather_task.navigation

import com.example.weather_task.feature.detailed_forecast.ui.DetailedForecastFragment
import com.example.weather_task.feature.main_screen.ui.MainScreenFragment
import com.github.terrakok.cicerone.androidx.FragmentScreen

/*

object LoginScreen : FragmentScreen {
    override fun createFragment(factory: FragmentFactory): Fragment {
        return LoginFragment.newInstance()
    }
}

class BluetoothDeviceSelectScreen(private val bluetoothDeviceType: BluetoothItem.BluetoothDeviceType) : FragmentScreen {
    override fun createFragment(factory: FragmentFactory): Fragment {
        return BluetoothDeviceSelectFragment.newInstance(bluetoothDeviceType)
    }
}

 */